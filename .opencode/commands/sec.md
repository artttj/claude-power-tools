---
description: Alias — security review via the security-reviewer agent
---

Dispatch the `security-reviewer` agent on the request below. Default scope: the current uncommitted diff or the file path the user names. Flag OWASP Top 10 issues, hardcoded secrets, SSRF, injection, unsafe crypto, missing authn/authz, and insecure defaults. Cite the exact line and rule for each finding.

$ARGUMENTS
