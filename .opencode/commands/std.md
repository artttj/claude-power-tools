---
description: Alias — apply baseline coding standards via the coding-standards skill
---

Use the `coding-standards` skill on the request below. Apply the baseline cross-project conventions for naming, immutability, file size, function size, error handling, input validation, and code-quality review. For framework-specific patterns, defer to `frontend-patterns`, `backend-patterns`, or `api-design` as the skill instructs.

$ARGUMENTS
