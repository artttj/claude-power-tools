---
description: Alias — simplify recently changed code via the code-simplifier agent
---

Dispatch the `code-simplifier` agent on the request below. The agent should focus on recently modified code (current diff or last edit) unless the user specifies a different scope. Preserve all behavior — this is a refinement pass for clarity, consistency, and maintainability, not a refactor.

$ARGUMENTS
